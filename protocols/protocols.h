#ifndef NETOS_PROTOCOLS_H
#define NETOS_PROTOCOLS_H

/**
 * @brief - Defines the protocol list.
 */
typedef enum {
    NETOS_PROTOCOL_ICMP     = 1,
    NETOS_PROTOCOL_TCP      = 6,
    NETOS_PROTOCOL_UDP      = 17,
} netos_protocol_t;

#endif

